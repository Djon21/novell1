# TODO — текущие хвосты

Оперативный список для `AVOS_S`. Здесь только живые задачи.

## Runtime / UI

- [ ] Подключить обработку `dm.get_effects()` в активный `v2` UI, чтобы реально работали Ink-теги `# sfx`, `# shake`, `# pulse`.
- [ ] Проверить полный цикл `exploration -> phone -> ink knot -> return_to_scene -> save/load`.
- [ ] Решить судьбу `click_001`: либо зарегистрировать в `sfx_player`, либо убрать из docs как неиспользуемый asset.

## Loop system

- [ ] Расширить loop-aware реплики за пределы `wake_intro`, `wake_after_choice` и финалов главы.
- [ ] Решить, растёт ли `loop_awareness` всегда на `+1` за конец главы или должен зависеть от выборов и найденных аномалий.
- [ ] Добавить удобный debug reset для `meta_state`, чтобы не чистить цикл вручную во время тестов.

## Exploration / сцены

- [ ] Добавить `exits` в `scenes.lua` там, где нужен `nav_buttons_v2`, и не держать всю навигацию только на hotspot'ах.
- [ ] Довести `city_map_hub` / карту мира до ясной боевой схемы: отдельная exploration-сцена или только modal map.
- [ ] Решить по локациям `metro`, `office`, `rooftop`: оставлять их линейными Ink-сценами или расширять point-and-click интерактив.

## Квесты / телефон / контент

- [ ] Проверить, что цепочка `make_coffee -> find_phone -> go_to_office` закрывается корректно и телефон показывает реальный прогресс.
- [ ] Убрать оставшиеся заглушки у phone apps (`calls`, `camera`, `terminal`) или явно привязать их к отдельным knot'ам.
- [ ] Довести achievements дальше текущих TODO-заглушек.

## Платформа / релиз

- [ ] Проверить интеграцию Яндекс SDK в реальной сборке, а не только по коду.
- [ ] Подготовить отдельный testing checklist для предрелизной проверки.
- [ ] Определить объём и приоритет локализации EN.
