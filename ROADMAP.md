# АВОСЬ — Roadmap

Игра эволюционирует из «визуальной новеллы с выборами» в **point-and-click adventure с RPG-элементами**: свободная навигация по комнатам/карте, инвентарь, квесты в телефоне, интерактивные hotspot-зоны. Ink остаётся как подсистема для диалогов/монологов, но перестаёт быть драйвером игры.

Целевая платформа — **Яндекс.Игры в браузере, ПК + мобильные**. Все интерактивные элементы должны быть достаточно крупными и визуально подсвеченными для тапа пальцем на мобильном.

---

## Решения по архитектуре

- **Game State Layer (Lua)** — единственный источник правды: inventory, quests, flags, current_scene.
- **Scene Graph (Lua)** — комнаты/локации как данные: bg + hotspot'ы + условия + on_click.
- **HUD** — всегда поверх сцены: иконки инвентаря / телефона / карты в правом верхнем углу.
- **Модалки** — инвентарь, телефон, карта открываются поверх сцены по клику на HUD-иконку.
- **Ink** — вызывается сценой для диалогов (`ink.play_knot("talk_to_mila")`). Тэги ink могут изменять game_state (`# flag:has_coffee=true`, `# item:add:keys`).
- **Инвентарь — взаимодействие Verb Coin (SCUMM-style):** клик на hotspot → появляется радиальное мини-меню «взять / посмотреть / использовать с…». Мобильно-дружественно.
- **Hotspot'ы — всегда видимые** (подсветка/пульсация/иконка), потому что игра в браузере на мобилках. Невидимые зоны не годятся.
- **Сохранения** — единое сохранение game_state + ink-state вместе, ещё в фундаменте (Спринт 1).

---

## Спринт 1 — Фундамент (базовая инфраструктура)

Цель: заложить game_state + scene_controller без ломания текущей игры. Спальня, квартира, метро, офис, крыша продолжают работать.

- [ ] `game_state.lua` — модуль с inventory/quests/flags/current_scene
- [ ] Единое сохранение: `game_state` + ink-state в одном save-файле
- [ ] `scene_controller.lua` — движок сцен: рендер фона, обработка кликов по hotspot'ам
- [ ] `scenes.lua` — каталог сцен (данные), стартовая сцена `apartment_hub`
- [ ] Ink-тег `# explore:scene_id` — передаёт управление scene_controller'у
- [ ] Ink-тег `# flag:name=value`, `# item:add:id`, `# goto_scene:id`
- [ ] Переделать текущий `apartment_hub` в ink под новый формат (минимальный — 3 перехода)
- [ ] Инструмент отладки: флаг `DEBUG_HOTSPOTS` рисует полупрозрачные рамки зон

## Спринт 2 — Комнаты квартиры + интерактив

Цель: свободное хождение по квартире с pickup'ами и условиями выхода.

- [ ] Сцена `kitchen` (`bg_kitchen`) с hotspot'ами: кофеварка, стол, выход в коридор
- [ ] Сцена `bathroom` (`bg_bathroom`) с hotspot'ами: раковина, телефон-полочка, выход в коридор
- [ ] Сцена `bedroom_day` (использует `bg_bedroom`) для возврата в спальню
- [ ] Pickup-механика: hotspot с `on_click = add_item(id)` добавляет предмет в инвентарь, hotspot исчезает/заменяется
- [ ] Условие выхода из квартиры: `flags.has_coffee and inv.has("phone")` — разблокирует hotspot двери
- [ ] Hotspot disabled-состояние (визуально тусклый) если условие не выполнено

## Спринт 3 — HUD + инвентарь

Цель: видимый инвентарь с иконкой в углу, открывается, предметы применимы.

- [ ] `hud.gui` — три иконки в правом верхнем: инвентарь, телефон, карта
- [ ] Скрытие HUD в меню, эндингах, диалогах-монологах где не к месту
- [ ] `inventory_modal.gui` — сетка слотов, иконки, тултипы с названием/описанием
- [ ] Иконки предметов: `item_coffee_cup`, `item_phone`, `item_keys`, ... (каталог в `items.lua`)
- [ ] Verb Coin: клик на hotspot → радиальное меню над hotspot'ом
  - Вариант жестов: короткий тап = «взять/действие по умолчанию», долгий тап = verb coin
- [ ] «Использовать предмет с …» — клик «использовать» в инвентаре → предмет становится курсором → клик по hotspot применяет

## Спринт 4 — Телефон + квесты

Цель: телефон с живым журналом квестов.

- [ ] `phone_modal.gui` — стилизация под смартфон (экран, иконки приложений или просто список)
- [ ] `quests.lua` — каталог квестов: title, steps (каждый шаг — предикат над game_state)
- [ ] Квест «Утро»: выпить кофе + взять телефон → разблокирует выход
- [ ] Реактивное обновление: шаги квеста проверяются при каждом изменении game_state
- [ ] Уведомления: «квест обновлён» / «квест завершён» — всплывашка в углу + звук
- [ ] Опционально: SMS/сообщения внутри телефона как ink-сценарии

## Спринт 5 — Глобальная карта

Цель: выйти из квартиры → карта города → свободный переход в метро/офис/крышу.

- [ ] `bg_city_map` — фон-карта (нужна картинка)
- [ ] Сцена `world_map` — hotspot'ы локаций: метро, офис, крыша, (и новые позже)
- [ ] Переход «выйти из подъезда» → `world_map`
- [ ] Сами локации (метро/офис/крыша) адаптируются: текущий ink остаётся, но вход и выход через сцену-контейнер
- [ ] Прогресс локаций: пройденная локация помечена на карте (изменённый hotspot, метка)
- [ ] Условия доступа локаций (по квестам): например, офис заблокирован пока не позвонили

## Спринт 6 — Полировка

- [ ] Видимые стрелки-иконки для навигационных hotspot'ов (вместо голых зон)
- [ ] Пульсация / edge-glow для hotspot'ов с активным quest-step
- [ ] Tooltip'ы при наведении (десктоп) / при долгом тапе (мобилки)
- [ ] Звуки: pickup, use item, open inventory, open phone, navigation
- [ ] Анимация перехода между сценами (слайд в сторону двери?)
- [ ] Инструмент редактирования hotspot-координат прямо в игре (drag-to-edit)
- [ ] Accessibility toggle «подсветить все интерактивные зоны»

---

## Что уже сделано (не в роадмапе — контекст)

- [x] Ink-интеграция (defold-ink), `chapter_01.ink` миграция
- [x] Портреты Артём/Мила, диалоговая панель
- [x] Фоны: bedroom/bedroom_01/02/03, apartment, menu, metro, office, office_evening, rooftop, kitchen, bathroom
- [x] Динамическая загрузка фонов через `play_flipbook` + crossfade 0.35s
- [x] Эффекты: sfx (stub), shake, pulse через ink-тэги
- [x] Terminal-панель нарратора (03:47, user@local:~$, мигающий курсор)
- [x] Гендерные токены `{MC|м|ж}`, `{NPC|м|ж}`
- [x] Базовое сохранение ink-state

## Что НЕ в роадмапе (back-burner)

- Звуковая библиотека SFX (terminal_wake, coffee_brew, heartbeat и др.) — ждём реальных файлов
- Озвучка / голос — решение потом
- Музыка — решение потом
- Глубокие мини-игры на телефоне — после базового телефона
- Локализация (сейчас только RU)
- Достижения Яндекс.Игр — после MVP

---

## Открытые вопросы

- **[Q3]** Прогресс шагов квеста: автоматически по флагам (реактивно) **или** явно объявлять в ink (`# quest_step:morning_routine:1:done`)? — *черновое решение: реактивно, но нужно подтверждение*
- Где рисовать сцены-hotspot'ы: в GUI-нодах или спрайтах game object'а? (GUI проще для HUD-слоёв, но хуже для параллакса; спрайты дают гибкость)
- Save-slot'ы: один auto-save или несколько слотов?

---

## Принципы разработки

1. **Мобильный first** — все кликабельные зоны ≥ 48×48 dp, явная визуальная подсветка.
2. **Не ломать то что работает** — каждый спринт должен оставлять игру играбельной.
3. **Данные а не код** — комнаты/предметы/квесты как таблицы, а не захардкоженные ветки.
4. **Ink остаётся** — для всего что про текст/выборы/повествование. Просто перестаёт рулить сценами.
