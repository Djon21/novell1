/.editor_settings
/.internal
/build
.externalToolBuilders
.DS_Store
Thumbs.db
.lock-wscript
*.pyc
.project
.cproject
builtins
# Defold liveupdate signing keys — приватный ключ никогда не коммитить
manifest.private.der
manifest.public.der

# Claude Code — локальные настройки (у каждого разработчика свои)
.claude/settings.local.json

# inklecate — бинарник Ink-компилятора (~57 MB), не храним в git.
# Скачать: https://github.com/inkle/ink/releases → inklecate_windows.zip
# Распаковать в tools/ (рядом с compile_ink.bat/sh).
tools/inklecate.exe
tools/ink-engine-runtime.dll
tools/ink_compiler.dll

# Бэкапы gui при перегенерации rebuild_menu.py
*.bak_menu
# Локальные копии из Claude Design (handoff bundle)
.design_*.html

# Подписные ключи и пароли — НИКОГДА в git
*.keystore
*.jks
*.p12
*.pfx
*.pem
*.key
*.pass.txt
*password*

# Переменные окружения / секреты
.env
.env.*
!.env.example
secrets.json
credentials.json

# Worktrees / служебные папки Claude
.claude/worktrees/

# IDE / OS
.idea/
.vscode/
*.iml
*.swp
*.swo
*~
desktop.ini

# Временные / бэкапные файлы
*.bak
*.tmp
*.orig
*.rej
*.log

# Python (на случай скриптов в tools/)
__pycache__/
*.pyo
.venv/
venv/

# Node (если появится package.json для тулинга)
node_modules/
npm-debug.log*

# Defold bob.jar (~182 MB) — не храним в git
scripts/bob.jar

# GigaIDE
.gigaide/
